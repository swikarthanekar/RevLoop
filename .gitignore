# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg

# Node
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo

# Environment and secrets
.env
.env.*
!.env.example

# IDE and OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Local deployment-CLI state
.vercel/

# Test and coverage
coverage/
htmlcov/
.coverage
playwright-report/
test-results/

# Build artifacts
dist/
build/

# Logs
*.log
npm-debug.log*

# Model artifacts are local-only, except the one bundle the deployed image
# loads at runtime (Dockerfile sets MODEL_BUNDLE_PATH to it).
*.joblib
*.pkl
!apps/api/app/ml/artifacts/recovery_model.joblib

# Presentation and build-process artifacts. Kept locally, deliberately not
# published: CURSOR_PROMPTS.md is the agent prompt sequence used to build the
# project, and the pitch script is word-for-word recording narration. Neither
# helps anyone understand, run or evaluate RevLoop.
CURSOR_PROMPTS.md
PITCH_SCRIPT.md
RevLoop-Demo-Script.pdf
